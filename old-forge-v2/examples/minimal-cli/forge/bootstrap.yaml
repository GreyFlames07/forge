bootstrap:
  description: Run the CLI status command through the canonical bootstrap path.
  required_units:
  - cli
  required_stores: []
  required_surfaces:
  - status_cli
  path:
  - status_cli
  success_criteria:
  - The CLI healthcheck returns ok.
  - The status command prints a JSON payload.
  preserve: true
