# Forge Minimal Web App Example

This example models a small Team Notes web app using the current Forge schema.

It includes:

- frontend UI container
- backend API container
- database container
- five container flows
- embedded C3 annotations for interface, logic, persistence, datastore, data
  shapes, and operations

The app is intentionally small. It is a schema and crawler fixture, not a
production implementation.
