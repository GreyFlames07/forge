schema: forge.v2.data_shape

data_shapes:
  - id: fulfillment_rejection_response
    description: Customer-facing rejection response returned when a fulfillment request does not proceed.
    kind: payload
    shape:
      order_id?: string
      status: enum[rejected]
      failure_reason: string
      user_message: string
