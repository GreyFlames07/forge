id: run_demo_job_flow
vertical: core
name: Run Demo Job Flow
description: Operator triggers the worker run-once path.
purpose: Keep a minimal worker execution path alive throughout the build.
trigger: Operator or harness runs the worker once.
path:
- kind: surface
  ref: run_demo_job_internal
- kind: operation
  ref: run_demo_job
side_effects: []
failure_modes: []
